#include <stdlib.h>
#include <stdio.h>

#include "slide_line.h"

/**
 * slide_line - Prints out an array of integer, followed by a new line
 * 
 * @line: points to an array of integers containing
 * @size: elements, that must be slided & merged to the direction represented by direction
 * @direction: can be either: SLIDE_LEFT  - SLIDE_RIGHT
 */
int slide_line(int *line, size_t size, int direction)
{

}