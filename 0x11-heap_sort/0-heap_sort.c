#include <stdio.h>
#include <stdlib.h>
#include "sort.h"


/**
 * main - Entry point
 *
 * Return: Always 0
 */
void heap_sort(int *array, size_t size)
{

	if(!array || size < 1)
		return(0);

	



}
